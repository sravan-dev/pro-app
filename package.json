{
  "name": "tijuspro-lms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"node backend/server.js\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd frontend && npm install && npm run build",
    "migrate:mysql": "node backend/migrate-sqlite-to-mysql.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "livekit-server-sdk": "^2.9.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.22.5",
    "nodemailer": "^8.0.10"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "sql.js": "^1.14.1"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "react-router-dom": "6.28.0"
  }
}
